import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  namePriceButtonStack: {
    margin: '16px 18.75px',
  },
  nameText: {
    fontSize: '20px',
    fontStyle: 'normal',
    fontWeight: 700,
    lineHeight: 'normal',
  },
}));
