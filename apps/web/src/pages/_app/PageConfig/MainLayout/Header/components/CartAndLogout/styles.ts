import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  cardAndLogoutGroup: {
    gap: '72px',
  },
  button: {
    overflow: 'visible',
    padding: 0,
    background: 'none',
    border: 'none',
  },
  cartIndicator: {
    position: 'absolute',
  },
}));
